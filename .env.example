# Server
NODE_ENV=development
PORT=4000
CLIENT_ORIGIN=http://localhost:5173

# Database (MySQL — e.g. Aiven, Railway, or Clever Cloud managed MySQL)
DB_HOST=localhost
DB_PORT=3306
DB_USER=eduledger
DB_PASSWORD=
DB_NAME=eduledger
DB_SSL=false

# Auth
JWT_SECRET=replace-with-a-long-random-string-at-least-32-characters
JWT_EXPIRES_IN=8h
